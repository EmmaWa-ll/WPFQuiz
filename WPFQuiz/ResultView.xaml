<UserControl x:Class="WPFQuiz.ResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFQuiz"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid Background="Transparent">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="{Binding ScoreText}" FontSize="32" FontWeight="Bold" TextAlignment="Center" 
                       HorizontalAlignment="Center" Foreground="#DCCB8E"/>
            <Button Content="← Back to Main Menu" Click="BackToMenu_Click" Padding="10" HorizontalAlignment="Center"
                    Background="#7A613F" Foreground="White" BorderBrush="#DCCB8E" BorderThickness="1" />
        </StackPanel>
    </Grid>
</UserControl>
